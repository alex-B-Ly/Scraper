  <div class="container">
    <div class="row text-center">
      <h1>Entrepreneur</h1>
    </div>

    <div class="row">
      {{#each doc}}
      <div class="col-xs-10 col-xs-offset-1">
        <p>{{this.title}}</p>
        <p>{{this.link}}</p>
      </div>
      {{/each}}
    </div>
  </div>